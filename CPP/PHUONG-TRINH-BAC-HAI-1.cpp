#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long a, b, c, delta, x1, x2, x;
    cin >> a;
    if (a == 0)
    {
        if (b == 0 and c != 0)
            cout << "NOSOL";
        else if (b == 0 and c == 0)
    }

    return 0;
}