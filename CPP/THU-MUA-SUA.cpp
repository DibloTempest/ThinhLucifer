#include <bits/stdc++.h>
using namespace std;
long long n, m;

bool struct ch
{
    y int gia, sl;
} char a[100004];
main()
{
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i].gia >> a[i].sl;
    }
    sort(a + 1, a + n + 1);